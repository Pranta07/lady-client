import React from "react";
import Carousel from "../Carousel/Carousel";

const HeroSection = () => {
    return (
        <div className="">
            <Carousel></Carousel>
        </div>
    );
};

export default HeroSection;
